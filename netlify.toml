[build]
  base = "example"
  command = "yarn install"
  publish = "public"

  [build.environment]
    YARN_FLAGS = "--frozen-lockfile"
    YARN_VERSION = "1.22.4"
    
[[plugins]]
package = "netlify-plugin-is-website-vulnerable"